// Importing React library
import React from 'react';

// Importing the SelectRole component from the components directory
import SelectRole from '../../components/Landing/page';

// Functional component for the Home page
const Home = () => {
    // Rendering the SelectRole component
    return <SelectRole />;
}

// Exporting the Home component as the default export of this module
export default Home;
